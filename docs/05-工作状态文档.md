# 工作状态文档

## 1. 当前进展

当前项目已经完成以下核心部分：

- Linux 中转服务器注册服务
- Windows 一键启动程序
- macOS 一键启动程序
- 配置注入脚本
- 打包脚本

## 2. 这次重新设计后完成的 Windows 改动

围绕“缺少 OpenSSH 时自动安装，并且主程序显示流程与安装信息”这个目标，已经完成：

1. 保留并强化了 `OpenSSH Server` 自动安装逻辑
2. 增加了独立安装器 `InstallOpenSsh.ps1`
3. 增加了主控窗口详细日志区
4. 增加了安装日志文件切换显示机制
5. 增加了安装模式配置：
   - `hidden`
   - `window`
6. 增加了干跑时强制模拟安装流程的测试开关

## 3. 当前 Windows 结构

- `start-remote-ssh.bat`
- `RemoteSshApp.ps1`
- `RemoteSshWorker.ps1`
- `InstallOpenSsh.ps1`

职责分工：

- `RemoteSshApp.ps1`：主界面，负责中文展示
- `RemoteSshWorker.ps1`：提权后实际执行
- `InstallOpenSsh.ps1`：只负责 OpenSSH 安装

## 4. 已验证内容

### 4.1 PowerShell 脚本语法

已验证：

- `RemoteSshApp.ps1`
- `RemoteSshWorker.ps1`
- `InstallOpenSsh.ps1`

### 4.2 Windows 干跑正常路径

已验证：

- 主流程可走通
- 状态文件正常输出
- 结果文件正常输出
- 日志文件正常输出

### 4.3 Windows 干跑强制安装路径

已验证：

- 即使当前机器已安装 OpenSSH，也可通过演练模式强制进入安装流程
- 会生成 `install-openssh.log`
- 安装步骤会正常完成
- 最终结果仍能成功输出

### 4.4 服务端接口

已验证：

- `/health`
- `/api/enroll`
- `/api/heartbeat`
- `/api/revoke`

## 5. 当前仍未完成的外部项

1. 真实中转服务器部署还没有完全收口
2. 你的服务器之前在 SSH 配置变更后出现过 `sshd` 重启失败
3. GitHub 远端仓库地址还没有落到具体仓库名

## 6. 建议的下一步

1. 继续修复并完成真实服务器部署
2. 创建或指定 GitHub 仓库
3. 将当前项目整体推送到 GitHub
4. 再做一次真实 Windows 目标机联调
